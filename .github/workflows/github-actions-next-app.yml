name: CI

on:
  push:
  pull_request:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Use Node.js 18.x
        uses: actions/setup-node@v2
        with:
          node-version: '18.x'

      - uses: nrwl/nx-set-shas@v3
      - run: npm ci

      - name: Lint check
        run: npx nx affected:lint --base=${{ github.event.before }} --head=${{ github.sha }}

      - name: Run Tests
        run: npx nx affected:test --base=${{ github.event.before }} --head=${{ github.sha }}

      - name: Build Next.js App
        run: npm run build:next-app
