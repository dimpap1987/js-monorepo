# Development Environment Configuration
# Used when monitoring LOCAL (non-containerized) services
#
# NOTE: host.docker.internal is a special DNS name that resolves to the host machine
# from inside Docker containers. This allows containers to connect to services running
# on your local machine (outside Docker).

# PostgreSQL - Connects to LOCAL postgres on host machine
# Use your root .env credentials
DATABASE_URL=postgresql://user:mypassword@host.docker.internal:5432/my-db?sslmode=disable

# Redis - Connects to LOCAL redis on host machine
REDIS_URL=redis://host.docker.internal:6379

# Grafana
GRAFANA_ADMIN_PASSWORD=passwordhere
