FROM node:20-alpine

# Install Docker CLI to access docker socket
RUN apk add --no-cache docker-cli

WORKDIR /app

COPY scripts/redis-client-exporter.js /app/

EXPOSE 9122

CMD ["node", "redis-client-exporter.js"]
